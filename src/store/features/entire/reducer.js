const initialState = {
  name: 'entire',
}

function reducer(state = initialState, action) {
  switch (action.type) {
    case '':
      return
    default:
      return state
  }
}
export default reducer
